# 🎧 Audiobook & Media Generation Guide

This guide explains how to use our standalone Node.js tool to convert raw text documents into high-quality audio files with realistic, emotional voice generation.

## 🛠️ Step-by-Step Local Setup

Follow these exact steps to run the audiobook generator locally:

### 1. Install Dependencies
Open your terminal at the root directory (`islamopedia-books/`) and install all required modules:
```bash
npm i